let screen = document.querySelector(".screen");

const NUMBERS = document.querySelectorAll(".number");

//NUmber function when pressing a number
NUMBERS.forEach((number) => {
  //Number event listener
  number.addEventListener("click", () =>
    //add Number
    {}
  );
});
